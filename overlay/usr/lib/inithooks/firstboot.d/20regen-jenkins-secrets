#!/bin/bash -e
# regenerate jenkins secrets (keys will be auto-generated)

for generated in /var/lib/jenkins/secret.key /var/lib/jenkins/identity.key; do
    if [[ -f $generated ]]; then
        find "$generated" -maxdepth 0 -type f -delete
    fi
done

# restart jenkins to ensure keys are created
systemctl restart jenkins
